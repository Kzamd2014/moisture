// Template for secrets.h — this file IS committed; secrets.h is NOT.
//
// Setup: copy this file to secrets.h in the same directory, then fill in your
// real 2.4 GHz network credentials (the ESP32 cannot join 5 GHz networks).
//
//   cp secrets.h.example secrets.h
//
#pragma once

const char* WIFI_SSID     = "your-network-name";
const char* WIFI_PASSWORD = "your-password";
